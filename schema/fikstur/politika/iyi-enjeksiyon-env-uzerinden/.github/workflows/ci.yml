name: CI
on: [push, pull_request]
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - env:
          BASLIK: ${{ github.event.issue.title }}
        run: echo "$BASLIK"
      - run: echo ${{ inputs.depo }} ${{ github.event.workflow_run.id }} ${{ github.sha }}
      - uses: actions/github-script@v7
        with:
          script: |
            const t = context.payload.comment.body;
