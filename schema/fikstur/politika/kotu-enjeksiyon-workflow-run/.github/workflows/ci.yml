name: CI
on: 
  workflow_run:
    workflows: [CI]
    types: [completed]
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - run: echo ${{ github.event.workflow_run.head_branch }}
